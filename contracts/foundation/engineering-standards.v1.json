{
  "contract_version": "engineering-standards.v1",
  "layers": {
    "frontend": {
      "language": "TypeScript",
      "framework": "React",
      "build": "Vite",
      "test": "Vitest + Testing Library",
      "rules": ["tsconfig strict=true", "dependencies use exact versions and package-lock", "business pages call the shared API adapter", "no duplicated auth, retry or error parsing"]
    },
    "backend": {
      "language": "Go",
      "framework": "Fiber",
      "database": "database/sql + SQLite",
      "test": "go test + go vet",
      "rules": ["gofmt required", "stable v1 response envelope", "money uses integer minor units", "writes are authorized and idempotent", "business variables are resolved from businessconfig"]
    },
    "validation": {
      "language": "Python 3",
      "rules": ["UTF-8 mode", "machine-readable contracts", "standard library or pinned dependencies", "non-zero exit on gate failure"]
    },
    "windows_automation": {
      "language": "PowerShell 5.1 compatible",
      "rules": ["UTF-8 initialization required", "UTF-8 BOM and CRLF for Chinese scripts", "no brittle powershell -Command nesting", "external command exit codes checked"]
    }
  },
  "cross_layer_rules": [
    "Communicate through versioned contracts, not implementation internals",
    "Variable prices and operational rules must not be hardcoded",
    "Root-cause fixes precede downstream work",
    "Every non-trivial architecture choice has an ADR and falsifiable premises"
  ]
}
