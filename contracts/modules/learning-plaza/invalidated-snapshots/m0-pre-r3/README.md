# 学习广场 M0 / M0-R2 失效治理快照

这些文件保持原始字节，只从 active 验证目录移出，不得改写为通过。

- 原 M0 与 M0-R2 checklist 在最新权威集成基线中绑定的 `agent-collaboration.v1.json` SHA 已过期。
- 两次历史考试的 `completed_at` 均早于所绑定 checklist 的 `completed_at`，不能证明 ADR 0017 要求的先完成清单、后开考时序。
- 对应实现记录因此不再参与 active implementation record 门禁。
- 当前有效证据从 `IR-20260713-LEARNING-PLAZA-M0-R3` 开始。
