# Project Brain v2 M3 任务理解回执

- work item：`AIW-20260717-PROJECT-BRAIN-V2-M3-BOSS-DASHBOARD`
- R3 exact start authority：`d1531daed440d4776c22f58c127710eb20eb2128`
- M2 G0根因修复已独立Go并进入authority；本R3必须重新证明M3 rounding修复与新M2 G0语义共同成立，不能沿用旧候选Go结论。

我要交付的是离线、进程内、synthetic-only的生产级驾驶舱候选内核，不是已经联网或上线的老板页面。它必须在服务端验证会话和角色，读取并复核M2不可变证据链，只输出去标识聚合视图与解释性字段。

我会让匿名、伪造、过期、错audience、未授权角色、direct URL、API越权、export、写方法、未知路径、audit/run/snapshot篡改、定义漂移、小样本、危险维度和行级结构全部失败关闭。前端隐藏不是安全边界。

我不修改M1/M2/v1、registry、App、公共scripts或共享route，不接真实数据、凭据、网络、external auth、export、notification、deploy、timer或production，不打开`boss_dashboard_enabled`，不激活M4-M5。

只允许任务令的8类路径。任一越界、authority漂移、未验证事实、角色混同、敏感内容或门禁失败均停止并判No-Go。
