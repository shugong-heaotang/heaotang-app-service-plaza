# 健康大管家第一阶段开发任务通知书（预制待启动）

通知编号：`HM-TASK-20260711-001`  
下达人：服务广场平台集成负责人  
承接人：健康大管家负责人（正式派发时确认）  
子项目：健康大管家 `personal-consultation-inbox`  
当前状态：预制待启动；不是正式派发，不授权签收、考试或编码。

## 一、启动条件

以下条件必须全部满足，平台才能把本通知改为“正式下达”：

1. 生命导航二完成 M0—M4，取得 `LN-M4-001 Go`。
2. 平台完成首个试验复盘，并把有效改进更新到本通知和模板。
3. 建立唯一工作项、`codex/` 专用分支和干净工作树，不与其他活动工作项重叠。
4. 平台和内部依赖均保持 development Go。

## 二、任务目标与固定接口

登录用户从 `/services/health-manager` 提交个人健康咨询，通过本人历史列表查看 `pending` 等状态，并返回 `/services`。

| 项目 | 标准 |
|---|---|
| 服务 ID | `health-manager` |
| 创建咨询 | `POST /api/v1/health/consultations` |
| 本人历史 | `GET /api/v1/health/consultations?page=<n>&size=<n>` |
| 请求字段 | `patient_name`、`symptoms` |
| 身份 | 平台共享 JWT；用户 ID 只能来自会话 |
| 幂等 | POST 必须携带 `Idempotency-Key` |
| 初始状态 | `pending` |
| 隐私 | 只能读取本人记录；咨询正文不写结构化日志、不发送 AI |

本切片不做诊断、处方、急救替代、AI 建议、预约、收费、报告生成、家庭共享和医生处置闭环。页面必须明确“咨询收件不构成诊断、处方或急救服务”；正式法律文案未确认时采用平台批准的最小风险提示，不得自行声称医疗效果。

## 三、修改范围

首个工作项原则上只允许模块专属前端目录和本项目文档。共享样式不得作为多个板块的共同修改区；确需平台样式或后端调整时，先在 M1 提交事实证据和接口变更申请，由平台另建无重叠工作项。

禁止直接修改平台合同、共享认证/API SDK、响应信封、日志和部署基础设施；禁止接触生产、真实资金或不可逆数据。

## 四、M0—M4 小检查点

- M0 起飞认证：签收、工作项/工作树、两层依赖、当前检查单、100 分考试。通过前不得写业务代码。
- M1 接口事实：复现 POST/GET、本人归属、幂等、错误信封和敏感日志行为，提交适配设计；不做大面积 UI。
- M2 数据适配：类型化咨询 API、加载/空/错误/重放状态和单元测试。
- M3 页面闭环：表单、本人历史、状态反馈、免责声明、返回路径和组件测试。
- M4 集成验收：全量测试、构建、UTF-8/契约门禁、测试环境真实登录 UAT、回滚证据、实施记录和 Handoff。

每个阶段使用 `HM-M0-001` 至 `HM-M4-001` 单独交验。平台给出 Go 前不得进入下一阶段。发现身份、跨用户、健康隐私、AI 外发、契约漂移或同类问题第二次出现时立即停止受影响工作并报告根因，不得积累到 M4。

## 五、完成标准

入口—提交—本人历史—状态—返回主链路可用；跨用户记录不可见；重复提交和网络错误反馈确定；敏感正文不进入日志或 AI；自动化、构建、契约、编码和测试环境 UAT 全部通过；M0—M4、实施记录、检查单和考试证据完整。
