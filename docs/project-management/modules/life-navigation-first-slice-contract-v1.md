# 生命导航首切片内部契约 v1

首切片：导航申请与我的申请记录。固定入口 `/services/life-navigation`，使用 `POST/GET /api/v1/life-nav/records`，记录类型固定 `application`，用户只读本人数据，写入强制幂等。页面必须提供登录、空、加载、错误、限额、重放和返回服务广场状态。

生命导航只负责诊断、报告、推荐和记录；题库、评分权重、AI 建议、收费套餐和下游服务交易不属于本切片。表达不得作确定性命运、医疗、心理、法律或金融承诺。
