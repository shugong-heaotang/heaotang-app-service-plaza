# 保障商城 M1-CP1 合同冻结

本切片仅冻结责任边界、五类状态机与 `mall.api.v1` 安全规范。合同位于 `contracts/modules/protection-mall/m1/`，验证器完全离线。

保障商城和俱乐部商城必须显式标识销售、履约及售后责任；保障权益只在保障商城订单完成且售后关闭后具备资格。关键写入必须服务端授权、原子幂等、可追踪且脱敏审计；非法、越权、乱序、缺ID、缺幂等键和破坏性兼容变更均失败关闭。

M1-CP1 Go不授权业务实现、支付、部署或上线。
