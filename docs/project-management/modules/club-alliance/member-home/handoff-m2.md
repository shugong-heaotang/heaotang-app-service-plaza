# H2-M2 前端适配器 Handoff

- 端点合同：`GET /api/v1/clubs/member-home`，共享会话、严格 v1 信封、只读安全重试。
- 完成：会员摘要、俱乐部、待办、活动、动态 DTO 的严格映射；负数计数、未知俱乐部类型、非数组、旧信封全部失败关闭。
- 自动化：8 个定向测试；仅一次认证请求；禁止 mock、catch 和空数组回退。
- 未完成：后端端点、本人归属/数据库实现、父路由加载状态接线、测试服真实响应。
- blocks：CA-SC 后端完成和 plugin 路由路径释放后才能实现聚合端点；未部署前不得称真实数据接入完成。
- 结论：M2 前端半程检查点，待平台复核；M2 整体仍 Pending。
