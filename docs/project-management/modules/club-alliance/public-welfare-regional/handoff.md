# 公益创办审批与区域管理中心 C1 Handoff

## 结论

状态：`handoff-ready`。合同、前端和独立后端实现完成，本地门禁通过；尚未执行测试服部署和真实浏览器 UAT，因此不是 release/production Go。

## 交付内容

- 公益俱乐部不再提供普通直接创建语义，页面明确说明“申请—审核—批准后创建并开放”。
- 创办申请包含基本条件、使命、团队资格、资源、治理、风险和 P/D/C/A/R 五段计划。
- 申请人可查看本人申请状态及批准后创建的俱乐部编号。
- 俱乐部联盟页面新增区域管理中心：可查两类中心，可申请主任或副主任。
- 管理中心新增公益创办与区域岗位待审列表，审核意见必填。
- 前端统一通过 business API adapter 使用严格 envelope、认证、超时、请求关联和 Idempotency-Key。
- 后端提交和详细证据见 `handoff-backend.md`。

## 验证证据

- 机器合同断言：3/3。
- 新增前端及父页面定向测试：46/46。
- 前端全量：25 files，243/243。
- `npm run build`：通过。
- `npm run build:test-server`：通过。
- 后端 `go test ./...` 与 `go vet ./...`：通过。
- 两个最终治理检查单均 28/28、当前 SHA mismatch=0；两个 R2 随机考试均 100 分。
- `scripts/Test-ServicePlazaContracts.ps1`：通过（最终证据落盘后需再跑一次）。

## 后续门禁

- 测试服部署、合成账号端到端 UAT、回滚验证需独立验收工作项。
- 服务中心资料录入/维护、区域角色撤销与任期管理需后续独立合同。
- 不含支付、真实通知、生产数据或不可逆操作。
