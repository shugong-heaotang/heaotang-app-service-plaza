# 生命导航 M4 前端 UAT：No-Go

- 记录时间：`2026-07-11T01:57:34+08:00`
- 环境：测试环境
- 页面：`/app/service-plaza/services/life-navigation/`
- 总结论：No-Go
- 严重级别：Blocker

## 已通过

- 未登录访问生命导航精确路由成功，页面一级标题为“生命导航”。
- 真实 `AuthPanel` 可见；页面提供返回 `/services` 的路径和底部导航。
- `390x844` 移动视口和 `1366x768` 桌面视口均无横向溢出。
- 页面未显示验证码、JWT 或其他会话秘密。

## 未通过

- 登录后浏览器 UAT 没有取得成功请求证据，因此未验证浏览器中的登录、提交、成功后历史刷新、页面刷新后历史持久、错误恢复和返回完整链路。
- 后续只读数据库核查得到：`today_count=5`、`active_unused=0`。
- 遵守既定门禁，没有换号、清除计数、修改限流、恢复公开验证码或把 shell token 注入受控浏览器。

## 根因

原测试身份预算只统计本轮调用，没有在分配正式浏览器账号前读取该 test-only 身份已有的 UTC 日 send-code 消耗。API 与受控浏览器是隔离执行面，shell API 会话不能安全注入受控浏览器；浏览器 `AuthPanel` 所需的第二次短会话遇到已耗尽的当日配额。

这不是页面或 life API 功能失败。API UAT 已独立通过，但没有浏览器成功证据时不得推断页面已通过认证后 UAT。

## 重测条件

1. 等待 UTC 日计数自然重置，不清理数据库、不修改限流规则。
2. 重测前先只读确认指定 test-only 身份的当日计数和可用额度。
3. 只执行一次 `AuthPanel` send-code；OTP 通过安全测试渠道临时取得并输入，不写入报告、截图、仓库或聊天记录。
4. 完成浏览器登录、提交、成功后历史刷新、页面刷新后记录持久和返回 `/services`。
5. 重测结束清空浏览器会话，不保存 token。
6. 不重新部署；只有制品、路由资源或 `/ready` 与本证据发生漂移时，才重新进入备份与部署门禁。

## 精确重测范围

- 需要重测：认证后的浏览器主链路。
- 无需重测：已通过的 API 首次创建、重放、冲突、本人历史和跨用户隔离；若 `/ready` 或制品漂移则例外。
- M4 结论保持 No-Go，直到上述浏览器链路取得完整成功证据。
