# 生命导航 M4 前端 UAT：Go

- 初次 No-Go：`2026-07-11T01:57:34+08:00`
- 成功重测：`2026-07-11T09:37:00+08:00`
- 环境：测试环境 `https://heaotang.cn`
- 页面：`/app/service-plaza/services/life-navigation/`
- 结论：Go

## 成功路径

1. UTC 日额度自然重置后，仅使用批准的合成测试身份 `199****9992`；通过真实 `AuthPanel` 发送一次验证码。
2. OTP 仅从批准测试服务器安全通道临时读取并直接输入浏览器，未写入聊天、报告、截图或仓库；JWT 同样未持久化。
3. 浏览器登录成功，页面显示合成测试用户和“提交导航需求”“我的申请记录”。
4. 提交合成说明 `生命导航M4浏览器验收-20260711-0927`，页面显示“申请提交成功”。
5. 本人历史立即出现该记录，状态“已提交”，时间 `2026/07/11 09:37`。
6. 浏览器刷新后登录会话仍有效，合成记录继续存在，证明页面从真实 API 重载持久数据。
7. 点击“返回服务广场”后 URL 为 `https://heaotang.cn/app/service-plaza/services`，页面显示服务广场及三大核心入口。

## 继承的既有证据

- 未登录精确路由、标题、AuthPanel、390x844/1366x768 无横向溢出已通过。
- API 首次 201、同键重放 200、异载荷冲突 409、本人历史、跨用户隔离和 request-id 已通过。
- `/ready`、部署制品、目录 9 services/20 actions 未发现漂移，因此按重测计划未重复部署。

## 根因关闭

原 No-Go 根因是未在浏览器会话前预算测试账号既有 UTC 日 send-code 消耗。现已通过公共 OTP 容量门禁、自然重置、单次浏览器会话和安全内存 OTP 路径关闭；没有换号、清计数、改限流或注入 shell token。

## 边界

本 UAT 只证明生命导航第一切片测试环境主链路，不授权生产、真实身份、真实资金或不可逆操作。历史 API 夹具中曾含问号字符的旧合成备注仍按原数据保留，不影响本次 UTF-8 中文记录正确显示。
