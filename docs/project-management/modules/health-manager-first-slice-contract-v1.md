# 健康大管家首切片内部契约 v1

首切片：个人健康咨询提交与本人记录。用户登录后通过 `POST /api/v1/health/consultations` 幂等提交，通过分页 GET 只读本人记录，初始状态为 `pending`。咨询正文属于敏感数据，不进入结构化日志。

本切片不做 AI 诊断、医生预约、收费、健康报告生成或家庭共享。AI 同意、医疗免责声明、危急分流、供应商边界和保留规则未形成独立契约前，服务端不得把咨询正文发送给 AI，`ai_advice` 保持空值。
