# 健康大管家与俱乐部联盟通知书独立审计修订

- 日期：2026-07-11
- 工作项：`AIW-20260711-PARALLEL-NOTICES-AUDIT-FIX`
- 实现记录：`IR-20260711-PARALLEL-NOTICES-AUDIT-FIX-R3`
- 当前结论：任务包审计修订 Go；正式派发仍 No-Go

## 权威状态统一

独立审计发现根 README 已说明 L0-L5 和三板块 development Go，但旧 `START-HERE.md` 仍把 F6-F8 记为 Partial Go，并称板块需求未冻结；状态一页纸也仍写生命导航本人历史 UI 未实现。平台以当前机器注册表、测试环境和生命导航 M4 证据为准，统一为：

- F1-F8 与 L0-L5 内部底座 Go；
- 生命导航二 M0-M3 Go，M4 部署和 API UAT 通过，浏览器登录后 UAT 因 UTC 日验证码额度耗尽保持 No-Go；
- 健康和俱乐部两层依赖允许需求/接口事实工作，但通知审计缺口关闭前不正式派发，M2 前置未关闭时明确 No-Go；
- 旧周看板只作历史背景，当前工作以协作登记、任务通知、最近 Handoff 和当前环境证据为准。

## 跨业务依赖修正

健康和俱乐部的机器依赖图没有生命导航业务依赖。旧通知把 `LN-M4-001 Go` 写成业务启动依赖，会把可并行板块重新串行化。修订后依赖的是“平台发布并冻结首个试验复盘模板版本”，用于继承检查点、Handoff 和测试账号教训；这是一项平台治理版本依赖，不是生命导航领域依赖。

## 健康大管家裁决

已冻结：

- `POST /api/v1/health/consultations` 与本人分页 GET；
- 会话归属、201/200 重放/409 冲突、`id DESC`、空列表和当前分页回退；
- 复用已集成的 `BusinessApiAdapter.executeWithMeta`，不直接读取原始响应头；
- 合成非真实健康数据、固定测试环境风险提示、平台负责路由/部署。

M2 前仍须由独立平台工作项关闭：

1. `patient_name/symptoms` 的 trim、必填与服务端最大长度；
2. POST/GET DTO、稳定错误矩阵和 GET `Cache-Control: no-store`；
3. 咨询正文不进入日志、遥测、截图和报告的专项证据；
4. `app/src/modules/health-manager` 的精确平台路由挂载。

## 俱乐部联盟裁决

已冻结：

- 自建俱乐部使用现有 `GET /api/v1/clubs/search?type=standard`，不使用 `/clubs` 做前端标签伪筛选；
- 加入申请继续强制原子幂等；
- `club-manage` 保持 preview，模块不得自行升级 active；
- 本切片禁止调用任何资金、付费、账户、提现和分账路由。

M2 前仍须由独立平台/后端工作项关闭：

1. 本人申请状态接口与资源归属；
2. 审核写入幂等和并发相反决定语义；
3. standard 申请/批准错误依赖 family 容量配置；
4. 普通用户、跨俱乐部、错配 application ID 的 HTTP 级越权证据；
5. 成员、积分、状态和 commit 失败的事务回滚；
6. 稳定 400/403/404/409 机器错误矩阵。

## 派发规则

- 当前任务包仍为“待启动”，不授权签收、考试或编码。
- 平台首个试验复盘模板发布、负责人/工作项/工作树确认后，可以分别正式派发 M0/M1。
- M0/M1 只允许认证、事实复现、接口设计和变更申请；不得用前端假数据掩盖缺口。
- 上述各模块 M2 前置全部关闭并经平台验收后，才允许进入 M2；任何一项未关闭均保持 No-Go。
- 测试环境部署、备份和回滚由平台集成/运维负责，模块负责人交付模块构建、测试和 UAT 证据。

## 验证

- 三轮治理检查单均完整：R2 用于状态入口变更后的重新认证，R3 用于协作登记收口后的最终认证。
- 三轮随机考试均为 8/8、100 分；最终实现记录只引用 R3。
- `Test-ServicePlazaContracts.ps1`、`Test-TextEncoding.ps1`、实施记录校验和 `git diff --check` 在提交前复跑。
- 本切片未修改业务代码、后端、测试服务器、生产、资金或不可逆数据。
