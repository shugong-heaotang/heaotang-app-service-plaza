# 服务广场项目推进从这里开始

本文档是服务广场项目的执行入口。所有开发和接续工作先读根目录 `README.md`，再按本文档推进。

全项目依赖顺序和业务开发准入以 `docs/project-management/dependency-driven-master-plan-v1.md` 为准。

## 一、当前阶段

- 服务广场两层标准已经定版：服务接入清单 `service-plaza.v1` 和页面动作清单 `service-plaza.action.v1`。
- 单页 20 个动作已全部目录驱动；测试环境现场验收为 20 项、20 个唯一 ID，规划项统一禁用。
- 服务目录 9 项和页面动作 20 项已通过后端 API、前端、公开 Schema 和机器基线一致性验证。
- 当前仓库已有正式 React 前端、真实登录/JWT、共享元数据 API 适配层、90 项前端测试和后端全量测试。
- 三项真实写入、原子幂等、请求关联 ID 与跨用户健康数据隔离已经通过远程验收。
- L0-L5 与 F1-F8 内部公共底座均已完成并有测试环境证据。生命导航二 M0-M3 已 Go，M4 已完成部署与 API UAT，但真实浏览器登录后主链路因测试账号 UTC 日验证码额度耗尽保持 No-Go；额度自然重置后只重测浏览器，不重复部署。
- 健康大管家与俱乐部联盟通知包已预制，但独立审计发现治理状态、公共挂载点、接口和安全前置缺口，修订并复核前不得正式派发。

## 二、唯一执行顺序

1. 阅读根目录 `README.md`，确认架构边界和门禁规则。
2. 阅读 `project-status-one-page.md`，确认当前事实和阻塞。
3. 读取 `agent-collaboration.v1.json`、当前任务通知和最近 Handoff，只领取一个唯一活动工作项；旧周看板只作历史背景，不覆盖当前证据。
4. 开发前查看 `docs/decisions/`，不得绕过已生效的 ADR。
5. 实现、测试并留下可复核证据。
6. 更新 Handoff、问题池、门禁状态和本入口的“最近接续记录”。

不再为了推进而新增重复台账。只有新的事实、决策、风险或验收证据才能进入项目文档。

## 三、当前最小开发切片

1. 北京时间 08:00 后完成生命导航二 M4 单次真实浏览器登录、提交、刷新持久和返回路径重测；制品与 `/ready` 未漂移时禁止重复部署。
2. 平台统一本入口与状态一页纸，按独立审计修订健康、俱乐部任务包；两模块只允许在公共前置关闭计划明确后进入 M0/M1，M2 继续 No-Go。
3. 生命导航 M4 Go 后发布首个试验复盘模板版本，再由两个独立板块负责人分别签收、考试和开发；平台不代做板块业务。

## 四、门禁口径

| 门禁 | 当前结论 | 解除条件 |
| --- | --- | --- |
| F1 接口标准 | Go | 两份 v1 契约、20 项动作、9 项目录、测试环境和公开 Schema 均已验证 |
| F2 内部身份与会话 | Go | v1 信封、真实 scopes、原子验证码与远程验收通过 |
| F3 统一 API SDK | Go | 超时、取消、严格信封、关联 ID、受控重试和回归测试通过 |
| F4 内部动作与权限 | Go | 生命周期、目标协议、真实 scope 和权限审计事件通过 |
| F5 写入可靠性 | Go | 三项原子幂等、竞态、日限额和远程回放通过 |
| F6 板块并行准入 | Go | 自检、共享适配器、版本差异门禁、两层依赖和递归治理均已验证 |
| F7 可观测性与配置 | Go | request ID、动作遥测、功能开关、业务变量和双人审批已在测试环境验证 |
| F8 发布恢复 | Go | 发布前备份、隔离恢复和部署失败自动回滚均已真实演练 |
| 生命导航二 M4 | No-Go | 部署与 API UAT 已通过；等待验证码 UTC 日自然重置后完成浏览器登录后主链路 |
| 并行板块正式派发 | No-Go | 先关闭通知书审计列出的公共接口、安全与治理状态缺口，并发布试验复盘模板版本 |
| 外部合作认证 | No-Go | OIDC/PKCE、域名白名单和合规专项未完成 |

## 五、编码与 PowerShell 规则

- Markdown、JSON、YAML、HTML、CSS、JavaScript、TypeScript 使用 UTF-8 无 BOM 和 LF。
- PowerShell 脚本使用 UTF-8 BOM 和 CRLF。
- PowerShell 会话先执行 `scripts/Initialize-PowerShellUtf8.ps1`。
- 执行项目脚本优先使用 `scripts/Invoke-ProjectPowerShell.ps1`。
- 提交前运行 `scripts/Test-TextEncoding.ps1`，发现乱码、错误 BOM 或 PowerShell 解析错误时不得提交。

## 六、Handoff 最小记录

每次接续至少记录：日期、执行角色、范围、已完成、未完成、阻塞、门禁变化、验证证据、下一步和变更文件。

## 七、最近接续记录

- 日期：2026-07-11
- 执行角色：服务广场平台集成负责人、生命导航二负责人
- 范围：生命导航二 M0-M4、平台路由、测试环境部署/API UAT、并行板块通知包预审
- 已完成：F1-F8；生命导航二 M0-M3；M4 前后端部署、备份、回滚目标与 API 双用户隔离 UAT；健康/俱乐部通知书独立只读审计。
- 未完成：生命导航二 M4 浏览器登录后 UAT；首个试验复盘版本；健康/俱乐部公共前置缺口关闭与正式派发；外部 OIDC/PKCE。
- 下一步：验证码 UTC 日自然重置后完成生命导航二浏览器重测；随后冻结复盘模板和修订后的并行任务包。
