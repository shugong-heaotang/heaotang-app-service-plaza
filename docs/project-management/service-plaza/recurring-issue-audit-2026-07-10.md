# 重复问题根因审计 2026-07-10

本次按“两次触发规则”回溯当前开发。UTF-8 乱码、硬编码业务变量、虚假 scope 和单人配置写入都已出现两次以上，不能继续逐文件修补。

| 模式 | 系统性措施 | 当前结论 |
| --- | --- | --- |
| UTF-8/mojibake | PowerShell 初始化、API charset、跨仓库 BOM/严格 UTF-8/乱码模式扫描 | 已验证 |
| 硬编码变量 | 整数分、版本化变量、未配置失败关闭、家庭价格移除、默认服务费关闭 | 已验证，双人审批在测试环境通过 |
| 虚假权限 | Manifest、动作、真实 token scope 和后端授权联检 | 已验证 |
| 单人配置 | 录入/审核分离；旧写入口迁移前锁定 | 已验证：同人 403、异人批准生成版本、旧入口 409 |
| 规则不可发现 | 两仓库 README/AGENTS 首屏入口、治理输入指纹和实现记录声明 | 已实现，待连续切片复验 |
| 共享工作区覆盖 | 一任务一分支一工作树、范围互斥、平台路径所有权、受控 Handoff/合并 | 已实现制度与门禁；当前历史脏工作区由平台集成负责人迁移收口 |

项目负责人已明确报告：后序 Agent 曾把前序 Agent 已完成内容重新改乱。当前仓库在建立工作项制度时也存在 61 项已修改或未跟踪路径，且历史上未按独立工作项归属。这两个事实共同触发 `RI-AGENT-SHARED-WORKSPACE-OVERWRITE`；不得再把共享脏工作区当作正常并行方式。

每次第二次出现同类问题，Handoff 必须引用 `recurring-issues.v1.json` 中的模式 ID；不得只写“已修复”。
